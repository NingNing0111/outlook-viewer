# 部署脚本配置（可选，复制为 .env.deploy 后生效）
#
#   cp .env.deploy.example .env.deploy

# Docker Hub 用户名
DOCKER_USER=ningning0111

# 镜像名
IMAGE_NAME=outlook-viewer

# 目标架构，逗号分隔
PLATFORMS=linux/amd64,linux/arm64

# 额外标签（可用 ./build-push.sh -t v1.0.0 临时覆盖）
TAG=
